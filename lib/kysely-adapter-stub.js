// Stub — project uses Drizzle adapter; kysely adapter is never invoked.
// Replaces @better-auth/kysely-adapter during Next.js build to avoid a
// kysely version conflict where DEFAULT_MIGRATION_TABLE is not a public export.
module.exports = {};
